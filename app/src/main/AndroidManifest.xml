<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tcc.iteach">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignUpInstructorActivity" />
        <activity android:name=".SignUpStudentActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAYNCDRkaIY9Zx_yignUVIajUSbiXtSAIA" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".LocationActivity" />
        <activity
            android:name=".SearchForInstructorActivity"
            android:exported="true" />
        <activity
            android:name=".SearchResultsActivity"
            android:exported="true" />
        <activity
            android:name=".instructor_main"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".blackboard"
            android:label="@string/title_activity_blackboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".schedule"
            android:label="@string/title_activity_schedule"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".reservations"
            android:label="@string/title_activity_reservations"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".notifications"
            android:label="@string/title_activity_notifications"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".settings"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".editSpots" />
        <activity android:name=".ViewInstructorProfile" />

        <service android:name=".FCMService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".notifications2"
            android:label="@string/title_activity_notifications"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".reservations2"
            android:label="@string/title_activity_reservations"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".settings2"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".student_main"
            android:label="ITeach"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".blackboard2"
            android:label="@string/title_activity_blackboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ReserveInfo" />
        <activity android:name=".Reserve" />
        <activity android:name=".answerActivity" />
        <activity android:name=".askActivity" />
        <activity
            android:name=".PaymentActivity"
            android:exported="true" />
    </application>

</manifest>